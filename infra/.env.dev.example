FRONTEND_PORT=5000
RELAY_PORT=5001
BACKEND_PORT=5002
RELAY_TOKEN=alf-dev-token
REPOS_ROOT=/home/juska/repos
DB_PATH=/home/juska/repos/alf/data/dev/alf.db
# Cursor SDK — required for agent turns. Get a key at cursor.com/dashboard/integrations
CURSOR_API_KEY=
# Default model when the UI doesn't pick one (e.g. composer-2.5, claude-haiku-4-5)
CURSOR_MODEL=composer-2.5